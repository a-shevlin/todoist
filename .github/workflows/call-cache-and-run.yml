name: TEST Call Cache and Run on Push
on: pull_request

jobs: 
  initialize-project:
    runs-on: ubuntu-latest
#    defaults:
#      run:
#        working-directory: frontend
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Check files in github actions
        run: |
          ls ${{ github.workspace/ }}
        
      - name: Setup Node Cache
        uses: ./.github/actions/setup-node@a-shevlin-patch-1
        
      - name: Build Dist
        run: npm run build
        
      - name: create dev instance
        run: npm run dev
    
    #TODO: implement remaining firebase 
